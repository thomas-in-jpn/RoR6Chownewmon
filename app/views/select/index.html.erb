<h1 class="display-4"><%= @title %></h1>
<p><%= @msg %></p>
<%= form_tag(controller: "select", action: "index") do %>
  <%= select_tag('s1', options_for_select(["Windows","macOS","Linux"])) %>
  <%= submit_tag("Click", {class: "btn btn-primary"}) %>
<% end %>